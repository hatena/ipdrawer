language: go
sudo: false
go:
- 1.8.x
- 1.7.x
go_import_path: github.com/taku-k/ipdrawer
cache:
  directories:
  - $HOME/local
before_install:
- go get github.com/golang/dep/cmd/dep
install:
- dep ensure
script:
- make test-all 
env:
  global:
  - "PATH=$PATH:$HOME/local/bin"